# ATLAS — Журнал Мандрівника

> Мультисторінковий журнал-сайт для мандрівників. Побудований на чистому HTML/CSS/JS без фреймворків.

---

## 📁 Структура проєкту

```
atlas/
├── index.html          # Єдиний HTML-файл (SPA-архітектура)
└── img/                # Всі зображення сайту
    ├── logo.png
    ├── LOGO-SITE.png
    ├── ліс-туман.jpg
    ├── Ісландія.jpg
    ├── Норвегія.jpg
    ├── Грузія.jpg
    └── ...             # ~40+ зображень
```

---

## 🗂 Сторінки

| Сторінка | ID | Опис |
|---|---|---|
| Головна | `home` | Герой, featured-статті, напрямки, цитата, гіди, поради, сезони, розсилка |
| Напрямки | `destinations` | Каталог із 12 напрямків та фільтрацією по регіонах |
| Гіди | `guides` | Featured-картки + сітка статей |
| Квіз | `quiz` | 5-крокова анкета → персоналізований результат |
| Калькулятор | `calculator` | Бюджетний калькулятор подорожі з динамічним результатом |
| Галерея | `gallery` | Masonry-галерея з фільтрами та лайтбоксом |

---

## ⚙️ Функціонал

### Навігація
- SPA-роутинг через `goPage(id)` — перемикання `.page` без перезавантаження
- Фіксований navbar з ефектом `solid` при скролі
- Прогрес-бар читання (лінія вгорі)
- Мобільне меню-оверлей
- Кнопка «Вгору»

### Пошук
- Оверлей з живим пошуком по 15 статтях
- Фільтрація по заголовку та тегу

### Статті
- Модальне вікно з повним текстом (`openArticle(id)`)
- 15 статей: репортажі, гіди, практичні поради
- Кнопки «Поділитися» (Twitter, Telegram, копіювати посилання)

### Квіз напрямків
- 5 питань (тип подорожі, клімат, бюджет, час, пріоритет)
- 12 можливих результатів на основі відповідей
- Перезапуск без перезавантаження сторінки

### Калькулятор бюджету
- 7 напрямків із реальними коефіцієнтами витрат
- Слайдери кількості днів та людей
- Перемикачі: переліт, харчування, тури, страховка, транспорт
- Динамічні bars-діаграми + поради з економії

### Галерея
- Masonry-колонки (4 → 3 → 2 → 1 залежно від ширини екрана)
- Фільтри: природа, міста, їжа, люди
- Лайтбокс із навігацією та анімацією

### Пасхалка
- Прихована кнопка у футері (opacity 0.04)
- Toast-повідомлення → Квест-модал

---

## 🎨 Дизайн-система

### Кольори
```css
--ink:    #0d0c0b   /* основний темний */
--cream:  #f5f0e8   /* світлий фон */
--warm:   #e8dcc8   /* тепло-бежевий */
--rust:   #b84c2b   /* акцент (CTA) */
--gold:   #c9952a   /* золотий акцент */
--sage:   #4a6741   /* зелений */
--pale:   #faf7f2   /* фон сторінки */
--mid:    #8a7e6e   /* другорядний текст */
```

### Шрифти
| Шрифт | Використання |
|---|---|
| `Playfair Display` | Заголовки, titling |
| `Cormorant Garamond` | Підзаголовки, body-текст |
| `Bebas Neue` | Великі числа, декоративний текст |
| `DM Sans` | UI-елементи, nav, кнопки |

### Кастомний курсор
- Маленьке коло `#b84c2b` у звичайному стані
- Збільшується до 52px при hover на інтерактивних елементах
- Прихований на мобільних (<600px)

---

## 📱 Адаптивність

| Брейкпоінт | Особливості |
|---|---|
| `> 1200px` | Повна версія, 4-колонкові сітки |
| `≤ 1200px` | 2-колонкові сітки, sticky calc відключається |
| `≤ 900px` | Мобільний nav, одноколонкові секції |
| `≤ 768px` | Зменшені шрифти, спрощений layout |
| `≤ 600px` | Мінімальний padding, курсор прихований |
| `≤ 480px` | Максимальна компактність |
| `≤ 380px` | Найменші екрани |

---

## 🚀 Запуск

Проєкт не потребує збірки — відкрий `index.html` у браузері або запусти будь-який статичний сервер:

```bash
# Python
python -m http.server 8080

# Node.js
npx serve .

# VS Code
# Розширення Live Server → Right click → Open with Live Server
```

---

## 🖼 Зображення

Усі фото з [Unsplash](https://unsplash.com) (безкоштовна ліцензія). Повний список використовуваних файлів — у директорії `img/`. При заміні зображень зберігай оригінальні імена файлів або оновлюй посилання в `index.html`.

---

## 📝 Додавання контенту

### Нова стаття
В об'єкт `articles` у JS додай:
```js
my_article: {
  tag: 'Категорія · Регіон · Країна',
  title: 'Заголовок статті',
  img: 'img/cover.jpg',
  meta: ['X хв читання', 'Тип', 'Місяць Рік'],
  body: `<p>Текст...</p><h3>Розділ</h3><p>...</p>`
}
```

### Нова картка напрямку
Додай `div.dest-full-card` з відповідним `data-region` у `#destFullGrid`.

---

*© 2026 ATLAS — Журнал Мандрівника*
